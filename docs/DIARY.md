# Дневник работ: МнеНеЖалко (бот + Mini App)

Подробная версия дневника: что сделано, зачем, почему именно так, и чем это проверено.
Человеческая версия для заказчика лежит рядом: `docs/mnenezhalko-diary.html`.
Порядок строго хронологический: и дни, и заходы внутри дня идут по возрастанию времени,
чтобы читалась логика развития. Самое свежее всегда в конце.

Прод: https://mnenezhalko-46-224-220-94.sslip.io (бот @mnenezhalkobot)
Код: TypeScript, Fastify 5 + grammY + Prisma/SQLite, web = React 18 + Vite
Репозиторий: https://github.com/ipostatos/mnenezhalko

---

## Как ведётся эта работа (три правила, которые объясняют почти все решения ниже)

1. **Проверять на живом, а не на бумаге.** Каждое изменение инфраструктуры и каждая правка
   безопасности проверяется реальным прогоном на проде (curl, логи, замер), а не только чтением
   кода. Причина: на соседнем проекте скрипт деплоя, «правильный по логике», уронил прод на 8 часов.
2. **Каждый баг сначала воспроизвести тестом.** Потом починить. Потом откатить починку и убедиться,
   что тест падает. Тест, который не падал до фикса, не охраняет ничего: он лишь фиксирует, что
   функция существует.
3. **Данные принадлежат проекту, а не приложению.** Источник правды по книгам это открытая таблица
   Notion, которую ведут люди. Приложение читает её и умеет писать, но не подменяет собой.

---

## 22 июля 2026. Запуск в прод и первый аудит

### Развёртывание
- VPS, systemd-юнит `mnenezhalko`, порт 4310, Caddy как фронт с TLS.
- Служба работает **от отдельной учётки** `mnenezhalko` (не root), с `NoNewPrivileges` и
  `ProtectSystem=full`, пишет только в `server/data`.
  *Зачем:* компромисс бота не должен становиться компромиссом сервера.
- `.env` принадлежит `root:mnenezhalko` с правами `640`.
  *Почему так:* файл читает systemd при старте, а служба должна иметь право чтения, но не записи.
  Без этого Prisma ловит `EACCES`.
- Бэкап `scripts/backup.sh` (база + обложки + `.env`, хранение 14 дней) в кроне root в 3:34.
  Проверено восстановлением, а не только фактом создания архива.
- ⚠️ Грабля: `.gitattributes` держит `*.sh` и `*.service` в LF. Без этого `git archive` с Windows
  отдаёт CRLF, и скрипт на сервере просто не стартует.

### Фото книги как основной способ добавления
Фото обложки уходит в Claude vision, оттуда название, автор, язык, жанры. Карточка на
подтверждение, затем запись владельца в Owners и книги в All Books.

*Зачем:* библиотека растёт только если добавить книгу проще, чем не добавить. Форма на 6 полей
убивает поток.

⚠️ Грабля: Anthropic SDK с картинками падает `APIConnectionError` на Windows/Node 25 (сырой fetch
тем же телом проходит), на VPS (Node 22) работает. Вывод: vision локально не тестировать.

### Запись в Notion
Таблицы расшарены на запись, но анонимно писать нельзя (`saveTransactions` отдаёт 401), поэтому в
проде лежит cookie личного аккаунта (`NOTION_TOKEN_V2`).
*Почему так, а не «официально»:* официальный API не покрывает нужные операции для этих таблиц.
Плата за это: cookie живёт около года и слетает при смене пароля, поэтому появился сторож
(проверка при старте и раз в сутки, сообщение админам при поломке и при возврате в норму, негативный
путь проверен подменой токена).
🔴 Долг: служебный аккаунт Notion вместо личного.

### Бот отвечал на один вопрос десятками сообщений
*Причина:* дефолт grammY `webhookCallback` (`onTimeout: 'throw'`, 10 секунд). ИИ-подбор не
укладывался в 10 секунд, вебхук отдавал 500, Telegram повторял апдейт около двух часов, и каждый
повтор давал новый ответ.
*Лечение:* `{ onTimeout: 'return', timeoutMilliseconds: 8000 }`, дедуп по `update_id` (15 минут),
single-flight на ИИ-подбор по пользователю, `setWebhook(..., drop_pending_updates: true)`.
*Диагностика на будущее:* 500 на `/tg/webhook` в `journalctl` и `pending_update_count` в
`getWebhookInfo`.

### Аудит безопасности (первый, внутренний)
- Вебхук требует `secret_token` (выводится из `BOT_TOKEN`). До этого подделкой `from.id` можно было
  выполнить админские команды и разослать анонс всем участникам.
- `/api/ai` и `/api/recognize` только по подписи Telegram + лимит на человека. До этого `/api/ai`
  был открыт всему интернету, а ключ Anthropic **общий с другим проектом**: чужой трафик оплачивался
  бы из общего кошелька.
- `isAdmin` перестал «залипать» в базе: единственный источник правды это `ADMIN_IDS` в окружении.
- Локальный запуск с `BOT_MODE=polling` делает `deleteWebhook` и молча забирает апдейты у прода.
  Добавлен предохранитель: увидев у токена чужой вебхук, сервер не стартует
  (осознанный обход `ALLOW_STEAL_WEBHOOK=1`).

### Технический долг того же дня
Кэш справочников (119 мс, стало 2 мс), поиск ранжирует весь набор совпадений (до 500) и режет на
страницы после, CI на GitHub Actions (типы сервера + сборка web + `npm audit`).

---

## 24 июля 2026. Стабилизация PL: план из восьми задач

Принцип, заданный заказчиком: сначала довести польскую версию до стабильной, потом масштабировать.
Восемь задач оформлены как GitHub Issues с зависимостями и критериями приёмки. Порядок не случаен:
первая задача это фундамент, остальные на ней стоят.

### 1. Единая идентификация библиотекаря (корень)
*Симптом:* полка библиотекаря пуста, хотя книг 91. Плюс дубли библиотекарей в списке.
*Причина:* `putOnShelf` искал человека по `tgId`, а синхронизация заводила его по `notionId` без
`tgId`. Получались две записи на одного человека, и Telegram-аккаунт висел на пустой.
*Решение:* `librarian.ts::linkLibrarian()` как единая точка опознания (tgId, затем нормализованный
ник с привязкой tgId, затем создание). Через неё ходят все пути: `putOnShelf`, `/api/me`,
`/api/my-books`, `/lend`.
*Миграция:* `npm run migrate:librarians` сливает дубли на главную запись, переносит книги,
привязывает `tgId`, архивирует дубль через `mergedIntoId`.
*Главный риск и как закрыт:* синхронизация могла вернуть книги назад на архивный дубль. Поэтому
sync резолвит `mergedIntoId` в `ownerByNotion`. Проверено реальным прогоном на копии dev-базы.
*Порядок на проде:* deploy (`db push`) → backup → stop → миграция → chown → start. Бэкап
`20260724-1551`.

### 2. Автообновление Telegram-контакта
При смене ника контакт правится сразу локально, отправка в Notion идёт в фоне. При сбое ставится
флаг `telegramSyncPending`, дожимается в синке, админам уходит сообщение. Синхронизация не
перезаписывает контакт у записей с невыполненной отправкой (иначе Notion затирал бы свежий ник).

### 3. Модерация книг
`reviewStatus` (pending / approved / rejected / deleted) **отдельно** от `notionStatus`.
*Почему отдельно:* «одобрена ли книга людьми» и «доехала ли она до Notion» это разные оси, и
смешивать их значит рано или поздно показать в каталоге неодобренное или потерять одобренное.
За флагом `MODERATION_ON` (включён на проде). Каталог фильтрует `approved` везде: поиск, карточка,
справочники, дайджест, профиль, счётчики, health.

### 4. Моя полка
Экран со всеми шестью состояниями (`shelfState()`), правка с отправкой в Notion, мягкое удаление
(`active=false` + `reviewStatus=deleted` + архивация строки), книга на руках не удаляется сразу, а
скрывается при возврате (`hideAfterReturn`). Все мутации: подпись Telegram + проверка владельца.

### 5. Проверка дублей книг
Ключ: владелец + нормализованное название/автор + тип. `own` (свой повтор) даёт предупреждение и
подтверждение при сохранении, `others` (чужой экземпляр) это подсказка, а не блок.
*Почему не блокировать чужие:* два экземпляра одной книги у разных людей это норма для
библиотеки, а не ошибка.

### 6. Кэш Mini App
*Найдено:* у `@fastify/static` при `cacheControl:false` его же `setHeaders` **не применяется**,
поэтому заголовки ставит единый `onSend`: `/api/*` no-store (кроме обложек со своим immutable),
`/assets/*` immutable, `/il/*` сутки, `index.html` no-store. Клиент шлёт `cache:'no-store'` и
перезапрашивает данные при возврате из фона (`visibilitychange`).
*Зачем:* Telegram держит вебвью в памяти, и без этого человек добавлял книгу, а на полке её не видел.

### 7. Подтверждение возврата, история, undo
Возврат в два шага, окно отмены 24 часа, модель `LoanEvent` (created / claimed / reminded /
returned / reopened / cancelled) как журнал событий выдачи.
*Зачем журнал:* без него «кто и когда отметил возврат» восстановить невозможно, а спорные случаи
в библиотеке обмена неизбежны.

### 8. Тесты напоминаний
Ядро `runOverdueReminders` вынесено из бота (без grammY), с инъекцией `now` и адаптера отправки.
10 сценариев на временной SQLite. Сбой отправки одному не срывает рассылку остальным.

### Дизайн-рефреш (в тот же день, отдельным заходом)
Фирменная палитра заказчика вместо тёмной темы ISSA: крем `#f7f1e8`, акцент персиковый `#eba788`
(цвет волос девочки с иллюстраций; путь итераций: индиго → лаванда → терракота → персик),
единый градиент персик-шалфей, иллюстрации заказчика, иконки Lucide вместо кустарных SVG.
Только визуальные изменения, функции не тронуты.
⚠️ Для этого приложения фирменная палитра **заменяет** общий стандарт ISSA: не откатывать.

---

## 25 июля 2026, первая половина. Скорость, импорт, внешний аудит

### Конвейер обложек
*Проблема по слоям, а не одна:* большинство обложек это прямые ссылки на чужие хосты (livelib,
azbooka, amazon, прокси Notion), оригиналы по 1 до 3 МБ; витрина карусели грузила 80 книг сразу;
подборка была новой случайной на каждый заход; в карусели на каждом кадре читались офсеты всех
40 элементов (forced reflow).

*Сделано:*
- `imgcache.ts`: `proxyCover(url, w)` даёт подписанный `/api/img?u=…&w=…&s=<HMAC>`. Подпись
  покрывает **и ширину**, иначе это открытый ресайзер и SSRF-вектор.
- `/api/img` ресайзит через **sharp** в webp-превью нужной ширины, кладёт на диск, дальше immutable.
- Дедуп in-flight (N запросов одной обложки это одна загрузка), лимит 4 одновременных внешних
  скачивания, негативный кэш 1 час для битых.
- Метрики в ответе: `X-Image-Cache`, `X-Image-Origin`, `Server-Timing: img;dur=`.
- Витрина 80 → 12 книг, стабильна 30 минут (memo по городу на сервере).
- Карусель: офсеты меряются один раз, центр и два соседа eager с высоким приоритетом, остальные
  lazy, `will-change` только у близких к центру, skeleton до загрузки.

*Замер на проде:* azbooka 971 КБ → webp 320 = 46 КБ (минус 95%), MISS 748 мс → HIT 2 мс.
⚠️ Прежняя оценка «50 до 500 раз» была локальным микрозамером одного файла, а не p50/p95 у
пользователей. Теперь для честного замера есть заголовки.

### Пакетный импорт
- Telegram-альбом: фото с общим `media_group_id` копятся с дебаунсом 1.5 с и обрабатываются пачкой,
  в конце одна сводка (до 10).
- `/import` со списком «Название — Автор» построчно (до 30).
- ISBN: `isbn.ts::lookupIsbn()`, **OpenLibrary первым** (`jscmd=data`, отдаёт авторов именами),
  Google Books запасным.
  *Почему такой порядок:* Google Books без ключа отдаёт 429 даже с прод-IP.
  ⚠️ Русские издания в OpenLibrary покрыты плохо, часто `null`.
- Добавление админом **от имени** библиотекаря: `putOnShelf` получил `ownerLibrarianId` (книга
  принадлежит цели, `addedByTg` это админ для аудита, город берётся у цели), команда
  `/onbehalf @ник`, сброс `/onbehalf off`.

🔴 Ботовые команды юнит-тестами не покрыты (нужен реальный Telegram и vision), ждут живой проверки.

### Внешний аудит: три P0
1. **Выдача чужой книги.** `createLoan()` брал `bookId` из тела на веру. Теперь в
   `prisma.$transaction`: проверка владельца (`book.owner.tgId === ownerTg`), `approved`, свобода
   книги и отсутствие второй активной выдачи.
2. **Частичный Notion-sync мог скрыть половину библиотеки.** `deactivationGuard`: при объёме ≥20 и
   «пропаже» >15% деактивация пропускается, синк помечается подозрительным, админам уходит
   предупреждение.
3. **SSRF через coverUrl.** `net.ts`: `isPrivateIp` / `assertPublicUrl` (резолв DNS + проверка всех
   адресов и на каждом hop редиректа, `redirect:'manual'`) / `isSafeCoverUrl` на приёме.
   ⚠️ Грабля: `isPrivateIp(host)` на **имени** домена вернул бы true. Литеральный IP проверять по
   маске, имя по результату DNS.
4. **sharp 0.33 → 0.35.3** (libvips CVE-2026-33327 / 33328 / 35590 / 35591).

---

## 25 июля 2026, вторая половина. Сессия стабилизации (этапы 0 до 9) и выход в прод

Отдельный заход по большому плану заказчика: сначала стабилизация, потом фичи. Тестов было 18,
стало **96**. Каждый пункт ниже это не «сделал по чеклисту», а найденный баг с воспроизведением.

### Полный жизненный цикл обложек
- **`/api/loans` отдавал сырой `book.coverUrl`** мимо всего конвейера: без ресайза, без кэша и
  **без SSRF-проверки**. Теперь через `withCoverProxy`.
- **Один размер 640px на всё** заменён на реальные размеры отрисовки из styles.css:
  `LIST_W=96`, `CARD_W=220`, `CAROUSEL_W=320`. Варианта «full» нет: крупнее 128px CSS обложка в UI
  не показывается нигде.
- **Фото пользователя сохранялось сырым: без EXIF-strip и без rotate,** а ссылка публичная и уходит
  в общий Notion, то есть GPS с телефона утекал. Теперь `covers.ts::normalizeForStorage`
  (rotate по EXIF → resize 1600 → webp, метаданные не переносятся).
  ⚠️ GIF не трогаем: resize без `{animated:true}` схлопнул бы анимацию в один кадр.
- **Лимит размера проверялся после полного `arrayBuffer()`:** origin без `Content-Length` мог залить
  гигабайты в память до отказа. Теперь `readLimited` считает байты по стриму и рвёт соединение.
- **Кэш превью не чистился вообще** (на проде 181 МБ, 1510 файлов, 0 эвикшена). `housekeepImgCache`
  (60 дней / 2 ГБ, таймер 6 часов) не трогает файлы младше 5 минут, чтобы не удалить недописанную
  запись.
- **Витрина прогревалась первым посетителем** → `warmShowcaseCovers` в фоне на новую ротацию.

### Инварианты выдач
- **`reopenLoan` (undo) содержал реальную гонку:** проверка «не выдана другому» и запись были
  разными запросами, конкурентный `createLoan` пролезал между ними, и на книге оказывались **две
  активные выдачи**. Воспроизведено тестом (2 !== 1), закрыто транзакцией.
- **`markReturned` тоже не был транзакционным:** падение между шагами оставляло книгу занятой
  навсегда, без выдачи.

### Claim-токены (дыра в ссылках-приглашениях)
*Было:* ссылка `?start=loan_<id>` содержала сырой `loan.id`, который публичен и светится в API,
событиях и Notion. При этом `claimLoans()` запускался при **каждом** `/start` и по совпадению
username забирал **все** чужие невостребованные выдачи. Зарегистрировав в Telegram чужой
освободившийся ник, человек одним `/start` получал чужие книги и право отметить их возвращёнными.
*Стало:* `Loan.claimTokenHash` (unique) + `claimTokenExpiresAt`, токен 24 байта, в базе только
sha256, `claimLoanByToken` привязывает **ровно одну** выдачу, при несовпадении username отказ.
Legacy-ссылки работают только для выдач без токена и только при совпадении ника.
`issueClaimToken` перевыпускает токен для напоминаний.

### Предохранитель синка перестал маскировать сам себя
*Было:* подозрительный прогон всё равно двигал «последний успешный» baseline, то есть скрывал ровно
то, ради чего предохранитель и делался.
*Стало:* baseline двигается только при `booksOk && gamesOk && !suspicious`. Плюс books и games стали
независимыми (падение games не срывает уже прочитанные books, у упавшего источника деактивация
пропускается целиком). Пороги в env: `NOTION_SYNC_GUARD_MIN/PCT`.

### DNS rebinding (TOCTOU)
*Было:* `assertPublicUrl` резолвил DNS сам, а `fetch()` потом резолвил хост **второй раз**.
Атакующий DNS с TTL=0 отдаёт публичный адрес на проверку и приватный на соединение: проверка
проходит, соединение уходит внутрь сети.
*Стало:* `assertPublicUrl` **возвращает** проверенные адреса, а `imgcache.ts::pinnedAgent`
(undici `Agent` с `connect.lookup`) подключается именно к ним.
*Проверено эмпирически:* подмена lookup на 127.0.0.1 реально даёт ECONNREFUSED, то есть пиннинг
работает, а не игнорируется. ⚠️ Добавлена зависимость `undici` (движок, на котором и так работает
встроенный fetch).

### Наблюдаемость и бюджеты
- `X-Image-Cache` различает HIT / MISS / **NEGATIVE** (раньше всё было «MISS»), плохая подпись это
  400, а не 404.
- Структурные логи (host, ширина, байты, категория ошибки) **без полного URL**: в query чужих CDN
  бывают их собственные токены.
- Админская ручка `GET /api/admin/img-metrics`: счётчики, bucketed-длительности, разбивка ошибок
  origin, размер кэша.
- `showcase-budget.test.ts` через `app.inject` на реальном Fastify: дефолт ≤12, clamp до 16, ключ
  кэша учитывает лимит, обложки идут через `/api/img?w=320`. Регресс к 80 книгам или к оригиналам
  будет поймано.
- `docs/PERFORMANCE_BASELINE.md`: замеры сняты **с прода по SSH**, не из головы.

### Выход в прод
Ветка `audit/performance-security-cleanup` слита в main, запушена, задеплоена. Прод на `659c804`.
Порядок: бэкап (`20260725-0854`) → deploy → `db push` → tsc → chown → restart.
⚠️ **Грабля:** `db push` отказался применять новую колонку `claimTokenHash` (unique) без
`--accept-data-loss`. Проверил схему на проде: колонки нет, значений нет, терять нечего. Применил
осознанно, а не «чтобы прошло».
⚠️ Репозиторий переехал: `ipostatos/mnenezhalko` (был `-mnenezhalko`), remote обновлён.

*Проверено живьём после деплоя:* health, форсированный MISS в конвейере обложек (352 мс, корректный
webp через новый пиннинг, байт в байт как до изменения), синхронизация.

### Фоновый синк не запускался вовсе
*Найдено на проде:* `lastSync` показывал 25 часов при заявленном периоде 12.
*Причина:* `setInterval` отсчитывался от старта процесса, а деплой это рестарт. При нескольких
деплоях в день 12-часовой отсчёт сбрасывался раньше, чем доходил до конца, и фоновая синхронизация
не отрабатывала **ни разу**. Ничего не падало: данные просто тихо старели.
*Стало:* `nextSyncDelayMs(lastSync, now, syncHours)` считает срок от последней **успешной**
синхронизации (`syncState`), просроченная идёт через загрузочную паузу 30 секунд (сначала подняться
и начать отвечать, потом идти в Notion).
*Ловушка в собственном решении:* неудачный и подозрительный прогоны не двигают `lastSync`, поэтому
планирование «от lastSync» само по себе давало бы срок в прошлом, то есть повтор каждые 30 секунд и
непрерывный долбёж Notion. Отсюда `retryDelayMs`: 15 / 30 / 60 минут, но не дольше периода.
«Удачным» считается только прогон, продвинувший baseline (то же условие, по которому health
показывает `lastSync`).
*Проверено на проде:* синхронизация поднялась через 30 секунд после рестарта, `lastSync`
продвинулся, следующий прогон запланирован через 720 минут, библиотекарей стало 143.
7 тестов на планирование, включая «битая или будущая метка не блокирует синк навсегда».

### Приватность контактов (этап 9.2)
*Решение продукта (заказчик, 25 июля):* каталог остаётся публичным, контакты закрываются подписью.
*Почему именно так:* книги и так лежат в открытой таблице проекта, прятать их бессмысленно и вредно
для будущих витрин и ссылок. А вот контакты это личное.
*Что было:* `GET /api/books`, `/api/books/:id`, `/api/librarians/:id` и дайджест отдавали
Telegram-ник и инстаграм владельца **без всякой авторизации**: базу контактов всех 143
библиотекарей можно было забрать одним `curl`.
*Найдено сверх заявленного:* `GET /api/market` отдавал **сырые строки таблицы**, то есть числовой
`authorTg` и ник каждого, кто когда-либо публиковал объявление. То же с `createdBy` у встреч.
*Сделано:* `server/src/privacy.ts` (`redactOwner` / `redactCard(s)` / `redactMarketItem` /
`redactEvent`), применено в `/api/books`, `/api/books/:id`, `/api/librarians/:id`, `/api/digest`,
`/api/market`, `/api/events`. Контакты только при валидной initData. **Числовые tgId не уходят
клиенту вообще, даже подписанному:** писать человеку можно только по нику, а по числовому id бот
может писать первым, то есть это внутренний идентификатор, а не контакт.
*Поля не удаляются, а становятся `null`:* у клиента типы `string | null`, условие `owner.telegram &&`
просто не покажет кнопку. Удаление ключей заставило бы фронт различать «нет ника» и «ник скрыт»,
хотя ведёт он себя одинаково.
*CORS:* `origin: true` (отражение любого Origin) заменён на собственный origin. Mini App живёт на том
же домене, что и API, а в dev у vite стоит proxy на `/api`, поэтому кросс-доменные запросы не нужны
никому из своих.
*Тесты:* `privacy.test.ts`, 9 штук на реальном Fastify через `app.inject`. Ключевой не «поле скрыто
у книги», а **сплошное сканирование ответов всех публичных ручек** на утечку ника, инстаграма и
числового id: новая ручка, автор которой забыл про redact, свалит тест.
*Проверено откатом:* снял redact у барахолки, упали 2 теста из 9, вернул.
*Проверено на проде:* анонимно `telegram: null` при 1768 книгах в Варшаве; с валидной подписью
(сгенерил на VPS из прод-токена) ники отдаются; чужой Origin не получает `Access-Control-Allow-Origin`,
свой получает.

---

## 27 июля 2026. «Моя полка» в общем стиле

*Что было:* экран собственных книг остался с прошлого этапа самым служебным местом приложения:
белые прямоугольники без обложек, эмодзи вместо иконок, кислотные цвета статусов (`#4caf72`,
`#50a8eb`), не имеющие отношения к крем-персиковой палитре заказчика.

*Сделано:*
- Карточка книги получила **ту же форму, что карточка выдачи**: мягкая карточка с крупным
  скруглением, тенью и цветной полосой состояния слева. Слева обложка, справа название, автор, город.
  *Почему обложка важна:* полка это в первую очередь визуальный список, по корешкам книга узнаётся
  быстрее, чем по строке текста. Сервер обложку и так отдавал, экран её просто не показывал.
- **Цвета статусов переехали в фирменную палитру**: «на полке» зелёный, «на руках» терракота,
  «на проверке» приглушённый синий, «отклонена» красный, «ошибка синка» медовый, «удалена» серый.
- **Бейдж состояния на карточке убран.** Состояние уже сообщают цветная полоса и заголовок группы
  («НА ПОЛКЕ · 2»), бейдж был третьим повтором того же самого.
- **Иконки Lucide вместо эмодзи** на кнопках, как на главной. Удаление намеренно **не** растягивается
  на всю строку: деструктивное действие не должно выглядеть главной кнопкой карточки (общее правило
  `.btn.ghost.sm` давало ему 100% ширины, и «Удалить» была самой заметной кнопкой экрана).
- **Сводка сверху** тремя плитками (на полке / на руках / на проверке), как на главной, и **плашка
  «Добавить ещё книгу»** внизу: раньше добавить книгу с этого экрана можно было только при пустой
  полке, то есть ровно тогда, когда добавлять уже нечего.
- Подзаголовок экрана правки теперь честный: для книги на проверке или отклонённой правка **не**
  уезжает в общую таблицу, потому что записи там ещё нет.

*Чем проверено:* собранная версия приложения поднята локально с подставными данными и снята в
браузере на ширине телефона: семь книг во всех шести состояниях, пустая полка, экран правки.
Смотрел на реальную вёрстку, а не на код. Плюс три шага CI (типы сервера, 96 тестов, сборка
Mini App) и живая проверка на проде: домен отдаёт новую сборку.

*Что осталось по дизайну:* остальные экраны уже в общем стиле; следующий заход по дизайну имеет
смысл делать с живыми скриншотами и дизайнерками (см. таблицу ниже).

---

## 27 июля 2026, вторая половина. Три жалобы по добавлению книг

### 1. С фото бралась только одна книга

*Что было:* распознавание спроектировано как «одно фото — одна карточка»: модель просили заполнить
карточку книги. Фото стопки или полки корешками давало ровно одну книгу, остальные молча терялись.

*Сделано:* ответ модели стал **списком**. Каждая обложка и каждый корешок — отдельный элемент;
корешок помечается флагом `spine` (по нему легче ошибиться, и человек это видит в списке); одна и
та же книга, попавшая в кадр дважды, схлопывается; нечитаемое **не додумывается** — вместо этого
приходит примечание «три корешка справа не читаются». Бот показывает найденное списком с
подтверждением и ставит на полку все книги разом. В альбоме каждое фото тоже может дать несколько.

*Почему без обложки:* общий кадр стопки не является обложкой конкретной книги. Пять карточек с
одинаковым фото засоряют и каталог, и карусель, поэтому при нескольких книгах обложка не ставится,
и бот об этом прямо пишет.

*Чем проверено:* на живой модели, двумя кадрами. Коллаж из трёх реальных обложек каталога → три
карточки. Нарисованная полка из пяти корешков (вертикальный текст, как на настоящей полке) → пять
книг с верными авторами, языками и `spine=true`. Плюс 6 тестов на разбор ответа: дедуп, отсев
нечитаемого, чужие жанры, ограничение 12 книг, не-JSON.

### 2. Книга по ISBN не добавлялась

*Причина оказалась не в коде.* Проверил каталоги руками: OpenLibrary **не знает** русских и
польских изданий (пустой ответ), а запасной Google Books на анонимные запросы отдаёт `429` —
общая квота на всех выбирается задолго до нас. То есть по ISBN находились почти только
англоязычные книги, и жалоба справедлива.

*Сделано:*
- источник **Biblioteka Narodowa** (`data.bn.org.pl`, без ключа) — польские издания; каталожную
  запись чистим до человеческого вида: «Kalanithi, Paul (1977-2015) Verghese, Abraham (1955- )
  Wydawnictwo Literackie» → «Paul Kalanithi», «Solaris / Solaris,» → «Solaris»;
- поддержан `GOOGLE_BOOKS_KEY` (бесплатный) — с ним появятся русские издания;
- **ISBN отдельным сообщением боту** теперь добавляет книгу. Раньше такое сообщение попадало в
  ИИ-подбор — бот отвечал на номер подборкой книг. Похоже, это и был наблюдаемый симптом;
- в Mini App добавлено **поле ISBN**: этого пути там не существовало;
- ненайденные ISBN перечисляются с советом (фото обложки или «Название — Автор»), а не тонут в
  «не разобрал N строк». Админу дополнительно видно, что упёрлись в квоту Google;
- обложка OpenLibrary проверяется HEAD-запросом: `?default=false` отдаёт 404, и без проверки в
  каталог попадала бы битая картинка.

*Ручка `/api/isbn`* закрыта подписью Telegram и лимитом: она ходит во внешние каталоги, открытым
прокси к ним быть не должна. Проверено на проде: анонимно 401, английская книга приходит из
OpenLibrary, польская из BN, русская честно отвечает «не нашёл, упёрлись в квоту», мусор — 400.

### 3. «Уже есть N экземпляров в <город>» врало

*Что было:* складывали **все** чужие экземпляры в одно число, а город брали **один** — с
наибольшим количеством. При книгах в разных городах получалось «3 экземпляра в Warszawa», хотя в
Варшаве стоял один.

*Сделано:* фраза строится по разбивке — «3 экземпляра — 2 в Warszawa, 1 в Kraków»; больше трёх
городов сворачиваются в «N в других городах»; экземпляры без города считаются отдельно и городом
не притворяются. Заодно город берётся у владельца, если у книги он пуст — частый случай для книг
из Notion, из-за которого экземпляры «теряли» город. Фразу собирает сервер, чтобы бот и Mini App
не разошлись.

*Чем проверено:* 6 тестов на реальной базе (разные города, город владельца, экземпляр без города,
своя книга не попадает в чужие, свёртка хвоста). Проверено откатом: со старым кодом падают ровно
эти тесты.

**Тестов стало 116** (было 96).

---

## 27 июля 2026, вечер. Ключ Google Books, меню бота

### Ключ Google Books подключён — русские издания по ISBN находятся

Заказчик прислал ключ, он лежит в `.env` на сервере (`GOOGLE_BOOKS_KEY`, права `640 root:mnenezhalko`,
в репозиторий не попадает). Проверено на проде: `9785171147426` → книга приходит, `9785041693459` →
книга с обложкой.

⚠️ **Две особенности Google Books, которые пришлось обойти:**
- **503 `backendFailed` в 60–90% запросов.** Замер с прод-адреса: 6 сбоев из 10, в другом заходе
  11 из 12, при этом между сбоями тот же запрос отдаёт книгу. Это не квота и не ключ — нестабильный
  бэкенд. Поэтому запрос к Google повторяется до 4 раз с растущей паузой; 429 (квота) **не**
  повторяем — это отказ, а не сбой.
- **Затяжной сбой больше не выдаётся за «книги нет»**: в ответе появился `serviceDown`, бот и
  Mini App советуют повторить через минуту, а не «сфотографируйте обложку».
- **Русские названия часто приходят транслитом** («Istorija Rossii» вместо «История России»), автор
  бывает пустым — так эти издания лежат в библиотечных каталогах, на которых стоит Google. Карточка
  в любом случае показывается человеку на подтверждение, название правится до сохранения.

### Меню бота: книжный клуб и поддержка проекта

В главном меню бота добавлены **«🌸 Книжная Клумба»** (страница клуба, адрес в настройке `CLUB_URL`,
меняется без правки кода) и **«💛 Поддержать проект»** — та же карточка сумм, что у `/donate`.
Появилась команда `/club`, она же в списке команд Telegram и в `/help`.

---

## 27 июля 2026, вечер. Регрессия с обложками — и как её больше не будет

### Что произошло

Заказчик сообщил: в «Библиотеке» пропали обложки, карусель снова медленная. Проверил на проде за
минуту: `/api/books` отдавал ссылки вида `…&w=0`, `…&w=2`, и такая ссылка отвечала **400**.

### Причина — моя же правка от 25 июля (`696572a`)

Тогда `toCard` получил второй параметр «ширина превью»: `toCard(b, w?: number)`. Но в коде уже
существовало `rows.map(toCard)`, а `map` передаёт колбэку вторым аргументом **индекс элемента**.
Ширина превратилась в `0, 1, 2, 3…`. Дальше цепочка: ссылка подписывается для ширины «2» →
обработчик `/api/img` поднимает ширину до минимальных 64 → подпись считается уже для 64 → не
сходится → 400 на каждую обложку.

Сломаны были каталог, дайджест и полка библиотекаря. Поиск по запросу и карточка книги работали
(там ширина передавалась явно) — поэтому поломка выглядела выборочной и не бросалась в глаза при
беглой проверке.

*Честно про причину:* это не «внешнее обстоятельство», а мой недосмотр при правке
производительности. Тип `number` во втором параметре — ровно та форма, которую `map` заполняет
молча. Поэтому чинил не только симптом.

### Три эшелона, чтобы это не повторилось

1. **Ширина передаётся объектом** `{ w }`, а не числом: `rows.map(toCard)` теперь **не
   компилируется**. Именно компилятор показал все четыре сломанных места — раньше их не показывал
   никто.
2. **`proxyCover` нормализует** постороннюю ширину в размер по умолчанию и пишет в лог: даже при
   будущей ошибке ссылка останется рабочей, а не станет битой.
3. **`/api/img` принимает только ширины из белого списка** (три размера, других в интерфейсе нет).
   Прежний «подгоним под границы» был худшим вариантом: молча превращал ссылку в вечный 400.

**Тест сплошной** (`cover-links.test.ts`): проходит по ВСЕМ ручкам с обложками и проверяет ширину
в ссылках. Новая ручка со своим `map(toCard)` его свалит. Проверен откатом обоих эшелонов — падает.

### Карусель: почему «опять долго»

Причина отдельная. Витрина карусели жила только в памяти процесса, а деплоев сегодня было шесть.
Каждый рестарт = новая случайная дюжина книг = 12 холодных превью, и первый посетитель платил за
все промахи. В метриках после рестарта: **56 MISS против 2 HIT**, 24 запроса дольше двух секунд.

Теперь выбранная дюжина хранится в базе с тем же сроком 30 минут и после рестарта достаётся
оттуда — вместе с уже прогретым кэшем картинок. Плюс прогрев витрины через 5 секунд после старта.
Замер сразу после деплоя: 12 из 12 обложек — попадание в кэш, 3–10 мс.

### Заодно: место на диске

Диск сервера был занят на 98% (746 МБ свободно) — при почти полном диске падает и запись кэша
превью, то есть каждый заход становится промахом. Освободил 3.6 ГБ: архивные системные журналы
(3.2 ГБ) и кэш пакетов. ⚠️ Оставшиеся 21 ГБ — Docker-образы **другого проекта** (NestingCenter),
их не трогал.

---

## 27 июля 2026, поздний вечер. Этап 10: миграции базы и защита выдач

### У проекта не было миграций вообще

База приводилась к схеме через `prisma db push`: он подгоняет структуру «как получится», без
истории и без переноса данных. Пока схема простая, это работает, но шаг вроде «добавить колонку
и заполнить её из существующих строк» так не сделать, а новая unique-колонка требует
`--accept-data-loss` (об это уже спотыкались 25 июля).

Сделано: `20260727000000_baseline` (снимок текущей структуры, 177 строк SQL) и первая настоящая
миграция. `scripts/db-migrate.sh`: на существующей базе без истории помечает baseline применённой
(структуру она описывает ту же самую), дальше накатывает обычным порядком; на пустой базе (новая
локация) создаёт всё с нуля. `deploy.sh` переведён с `db push` на этот скрипт.

### Запрет «две активные выдачи одной книги» переехал в базу

Проверки в `createLoan`/`reopenLoan` есть и обёрнуты в транзакции (гонку undo чинили 25 июля), но
это защита прикладного слоя: любой новый путь записи, забывший про проверку, снова разъедется.

Теперь у `Loan` есть `activeBookId`: копия `bookId`, пока выдача активна, и NULL у закрытой.
`@@unique([activeBookId])` делает второе такое состояние физически невозможным. NULL в SQLite не
конфликтует с NULL, поэтому закрытых выдач у одной книги может быть сколько угодно, а выдачи без
книги не мешают друг другу.

*Почему колонка, а не частичный индекс* `(bookId) WHERE status='active'`: частичный индекс нельзя
описать в схеме Prisma, и каждый следующий `migrate diff` считал бы его лишним и предлагал удалить.
Колонка живёт в схеме, ею управляет Prisma, дрейфа нет.

*Чем проверено:*
- репетиция на КОПИИ прод-базы (скачал живую базу): 3270 книг целы, перенос состояния верный,
  индекс создан, вторая активная выдача отбита базой;
- 2 новых теста (132 всего), проверены откатом: без `@@unique` тест «база сама не даёт» падает;
- на проде после миграции: `active` 2 из 2 занимают книгу, `returned` 7 из 7 свободны, и прямой
  `INSERT` второй активной выдачи даёт `UNIQUE constraint failed: Loan.activeBookId`.

⚠️ Порядок был: копия данных (`backup.sh`) → деплой → `migrate resolve --applied baseline` →
`migrate deploy` → рестарт. Первый шаг обязателен: миграция меняет структуру живой базы.

---

## 28 июля 2026. Закрытие независимого аудита

Полный технический отчёт: `docs/AUDIT_CLOSURE_2026-07-28.md` (20 коммитов, PR #9).
Здесь коротко, что важно помнить дальше.

- **Напоминания о возврате не отработали ни разу** с запуска: таймер жил в памяти
  процесса и обнулялся каждым деплоем. Теперь единый планировщик (`scheduler.ts`)
  хранит время последнего успешного прогона в базе; на нём же housekeeping кэша,
  чистка осиротевших обложек, протухание барахолки и фоновый прогрев превью.
- **Синк перетирал локальное состояние** (busy у выданных, города, addedAt,
  воскрешал удалённые). Правила владения полями зафиксированы в
  `docs/DATA_OWNERSHIP.md` и охраняются тестами. Смоук на проде показал фикс
  вживую: книга при активной выдаче, утром «освобождённая» старым синком,
  после первого нового синка вернулась в busy.
- **Грабля Notion API, пойманная смоуком**: на большой limit сервер отдаёт все
  blockIds с `hasMore=false`, но сами строки режет примерно до тысячи. Полнота
  теперь проверяется по числу строк против id (`isCompleteResponse`), неполный
  ответ шардируется по датам. Предохранитель деактивации в этот момент отработал
  ровно как задуман: подозрительный синк ничего не скрыл и не сдвинул baseline.
- **Модерация идемпотентна**: `pending → approving` атомарно, строки Notion
  создаются с детерминированным id (ретрай попадает в ту же страницу). Карточка
  уходит каждому админу, теперь их трое (добавлены Аня и Ира) — двойное
  одобрение штатно отвечает «уже одобрено».
- **/api/photo** закрыт подписанными ссылками (img-тег не умеет заголовки),
  выдачи не привязываются по нику (только claim-токен), время встреч считается
  по Europe/Warsaw, из одной афиши сохраняются все встречи.
- Mini App: полная пагинация с сохранением состояния при возврате из книги,
  починенные быстрые тапы, поиск по полке, «в других городах нашлось N».
- Тестов 132 → 209, подключён vitest для web, CI гоняет оба набора.
- Деплой теперь `npm ci`; удалён опасный `db:push` и одноразовая миграция.

*Чем проверено:* локальный gate перед каждым из 20 коммитов, CI на PR, бэкап
базы с проверкой целостности до деплоя, прод-смоук по чек-листу (журнал джоб,
ручной синк, 403 на фото-ручке, метрика прогрева 20.7% и растёт).

---

## 29 июля 2026. Оценки и отзывы к книгам (issue #18, PR #19)

Первая продуктовая фича после аудита. Коммит `68eb3b2`, миграция
`20260729090000_book_reviews`, на проде в ночь на 29 июля.

- **Рейтинг вешается на произведение, а не на экземпляр.** Ключ `workKey` —
  нормализованные название и автор. В каталоге у одной книги по карточке на
  каждого библиотекаря: при привязке к `Book.id` у пяти экземпляров «Мастера и
  Маргариты» вышло бы пять рейтингов по одной оценке. Проверено на копии живого
  каталога: все пять экземпляров показывают одну среднюю.
- **Право оценивать даёт только прочтение**: закрытая выдача этой книги на
  человека либо свой экземпляр на полке. Активная выдача права не даёт, чужому
  `403 not_read`. Один отзыв на человека на произведение гарантирован базой
  (`@@unique([workKey, authorTg])`).
- **Постмодерация вместо предварительной**: текст до 300 знаков, ссылки
  запрещены, кнопка «пожаловаться», три жалобы скрывают отзыв и убирают его из
  средней. Правка своего отзыва снимает жалобы. Наружу уходит только имя автора,
  без ника и tgId.
- **В Notion отзывы не пишутся**: источник правды по книгам — таблица проекта, а
  мнения читателей к данным каталога не относятся.
- Данные: `Review` + агрегат `WorkRating`, пересчёт в одной транзакции с
  отзывом. Бот спрашивает оценку один раз после подтверждения возврата (в том
  числе когда выдачу закрыли из Mini App), текст ждёт два часа.
- Попутно: ответ SPA-заглушки на **ещё не существующий** `/assets/index-<хэш>.js`
  уходил с `immutable` на год. Браузер человека, зашедшего между сборкой и
  рестартом, запоминал HTML под именем скрипта, и приложение оставалось сломанным
  до чистки кэша. Теперь «навсегда» получает только реально найденный файл.
- Тестов: серверных 223 (+18), плюс первые тесты интерфейса Mini App
  (vitest + jsdom + RTL, 9 сценариев экрана отзывов) — первый шаг по долгу
  TECH_DEBT 2.1. Всего с web-набором 239.

*Чем проверено:* локальный прогон обоих наборов, зелёный CI на ветке и на PR,
бэкап базы (`20260728-2351`) до деплоя, прод-смоук подписанными запросами:
`canReview` у владельца `true`, чужая непрочитанная книга `403 not_read`, оценка
9 `400 bad_rating`, ссылка в тексте `400 links_not_allowed`, создание и удаление
отзыва проходят и подчищают агрегат (после смоука в базе 0 отзывов).
Заодно проверен фикс кэша: несуществующий ассет отдаётся `no-store`, реальный
остаётся `immutable`.

⚠️ Грабля смоука, не бага: `DELETE` с заголовком `content-type: application/json`
и пустым телом Fastify отбивает `FST_ERR_CTP_EMPTY_JSON_BODY`. Mini App шлёт
`DELETE` без этого заголовка, поэтому удаление отзыва работает; воспроизводить
ручные проверки нужно ровно тем же набором заголовков, что и клиент.

---

## 29 июля 2026, вторая половина. Очередь на занятую книгу (issue #10, PR #20)

Коммит `00d0274`, миграция `20260729140000_waitlist`, на проде в тот же день.

- **Очередь висит на экземпляре, а не на произведении** — сознательно обратно
  тому, как устроены оценки. Оценивают текст (у пяти библиотекарей одна
  средняя), а ждут конкретную книгу у конкретного человека в конкретном городе:
  обещание рождается из возврата именно этого экземпляра.
- **Обещание переживает падение процесса.** Возврат книги и выбор следующего —
  одна транзакция (`promoteNextWaiting` внутри `markReturned`), запись остаётся
  в состоянии `ready`. Отправка идёт отдельным шагом: сразу после возврата и,
  если он не удался, джобой `waitlist` раз в час. Уведомление, живущее только в
  памяти обработчика, деплой в неудачную секунду съел бы молча — ровно та
  грабля, что была у напоминаний (аудит 28 июля).
- **Ждущие не видят друг друга**: наружу уходит только число ожидающих и своё
  место. Владельцу на «Моей полке» видно число. Тест сплошным сравнением
  проверяет, что в теле ответа нет ни одного чужого `tgId`.
- **Очередь не бывает вечной**: TTL 60 дней, не больше 20 книг на человека,
  а если позванного не было сутки и книга всё ещё свободна, очередь переходит к
  следующему (`escalateStale`). Иначе один молчун держал бы книгу в подвешенном
  состоянии, а остальные не узнали бы о ней никогда. Заблокировавший бота
  закрывается после трёх неудачных доставок.
- **Отмена возврата**: неотправленное обещание возвращается в очередь на прежнее
  место, уже доставленное не отзывается. Книга, скрытая владельцем после
  возврата, закрывает очередь молча.
- Mini App научился ссылке `?screen=book&id=<id>` — письмо ведёт прямо в
  карточку книги, а не на главную.
- Тестов: серверных 245, интерфейса 24.

*Чем проверено:* каждый тест проверен откатом фикса (без `promoteNextWaiting`
падает 6, без возврата обещания при undo и без закрытия очереди у скрытой книги
— по одному). Зелёный CI на PR, бэкап базы до деплоя, прод-смоук подписанными
запросами: аноним `401`, вставание в очередь `200` с позицией 1, свободная книга
`409 not_busy`, в карточке `waiting.count=1` и ни одного `tgId` в ответе, выход
из очереди `200`, джоба `waitlist` отработала («доставлено 0/0, протухло 0»).
Тестовая запись из прод-базы удалена.

**Заодно починено то, на что я сам наступил дважды** (`55bf367`): пустое тело
при заголовке `content-type: application/json` Fastify отбивал
`FST_ERR_CTP_EMPTY_JSON_BODY`. Удаление (отзыва, места в очереди) тела не имеет
по определению, поэтому любой клиент, ставящий заголовок по умолчанию, получал
400 на совершенно корректном запросе. Свой Mini App заголовок при пустом теле не
шлёт и потому не страдал — но ловушка сработала на двух ручных проверках подряд,
а значит сработала бы и на стороннем клиенте. Теперь пустое тело читается как
`{}`, кривой JSON остаётся ошибкой 400. Проверено на проде.

---

## 29 июля 2026, ночь. Значки, «сколько сберегли», счётчики-кнопки, новинки, подсказка ников

Четыре захода подряд: issues #11 и #13, две жалобы по интерфейсу и одна
собственная регрессия, пойманная прод-смоуком.

**Значки библиотекаря (#11, PR #21).** Семь штук: первая книга, полка на десять,
целая библиотека, первая выдача, книги ходят, читатель, первое мнение.
Считаются **на лету** из полки, выдач и отзывов: ни таблиц, ни миграций, ни
фонового пересчёта, значок негде «потерять». Видит только их хозяин — публичный
рейтинг превратил бы обмен книгами в соревнование. Незаработанные показываются
бледными и с прогрессом, а пока не заработан ни один, блок не рисуется вовсе:
витрина серых кружков для новичка это упрёк, а не подсказка. Пороги низкие
намеренно (риск из самого issue: у половины библиотекарей полка меньше десяти
книг).

**«Вместе мы сберегли» (#13, PR #21).** Обмены → злотые, килограммы бумаги,
деревья. Считаем обмены, а не книги на полках: полка это возможность, польза
случается, когда книга ушла из рук в руки. Коэффициенты честно занижены и
вынесены в константы с обоснованием (40 zł при рыночных ~45, 0.3 кг бумаги в
книге, 60 кг бумаги из дерева), рядом стоит «примерно», формула раскрывается
кнопкой — спорность методики известный риск, и прятать её хуже, чем показать.
При написании тестов вылезло: одно дерево это две сотни книг, и при округлении
до десятых первые полторы сотни обменов давали бы «примерно 0 деревьев». Теперь
два знака, а колонка показывается от 0.1 дерева. На проде сейчас 9 обменов —
360 злотых и 2.7 кг бумаги.

**Счётчики перестали притворяться кнопками (PR #22).** Жалоба user: «на главной
дашборд выглядит как кнопки, но нажать нельзя». Причина: палец-курсор и отклик
на нажатие стояли у ВСЕХ плиток `.stat-tiles`, а кнопками они были только на
главной — в «О проекте» и на «Моей полке» человек жал в пустоту. Теперь
интерактивный вид только у настоящих кнопок; в «О проекте» счётчики ведут в
библиотеку и города, на «Моей полке» работают фильтром по состоянию.

**Новинки за сутки (PR #22, потом #24).** Вопрос user: «почему не показывает за
24 часа». Данные оказались честными — последняя книга добавлена 27 июля, за
сутки не добавляли ничего. Но вскрылись два случая, когда книга не попала бы в
суточный дайджест никогда: в таблице проекта стоит дата **без времени** (книга,
вписанная вчера в 23:00, к утру «старше суток»), а сверка идёт раз в 12 часов
(книга, вписанная позавчера и подхваченная сегодня, старая по дате и незнакомая
по факту).

🔥 **И тут же собственная регрессия, пойманная прод-смоуком.** Первая версия
правки звучала «`addedAt` в периоде ИЛИ `createdAt` в периоде» — и месячный
дайджест на проде показал **весь каталог, 3249 книг вместо 81**: база на сервере
создана 22 июля, поэтому `createdAt` у всего каталога попал в 30-дневное окно.
Лечение (PR #24): вклад `createdAt` ограничен допуском на задержку сверки (трое
суток), то есть новинка это «мы узнали недавно И в таблице она тоже свежая».
Массовый импорт через это условие не проходит. После деплоя: за месяц 113, за
сутки 0 — то самое честное «сегодня никто ничего не добавлял».
⚠️ Урок на будущее: правку, расширяющую условие выборки, проверять не только
тестом на синтетике, но и живым числом с прода — синтетика не знает, что вся
база создана в один день.

**Подсказка ников (PR #23).** Просьба user: «нужно помогать вводить ник, а то
некоторые длинные и просто нереально записать вручную». В «У кого моя книга»
под полем появились подсказки по первым буквам ника или имени (задержка 250 мс,
до восьми). Граница приватности: подсказываем библиотекарей (их имена и ники и
так в открытой таблице проекта) и тех, кому спрашивающий уже давал книги (его
собственная история, такие идут первыми). Всех, кто просто открывал бота, — нет:
форма выдачи не должна превращаться в справочник ников. Наружу уходит только имя
и ник.
⚠️ Отбор идёт в памяти, а не запросом с `contains`: SQLite сравнивает LIKE без
учёта регистра только для латиницы, поэтому «Лиза» не нашла бы «Лиза Ж», а
половина имён в проекте кириллические.

*Чем проверено:* тестов стало 280 серверных и 35 интерфейсных, каждый новый
проверен откатом фикса. Зелёный CI на всех четырёх PR, бэкап базы перед каждым
деплоем, прод-смоук после каждого: значки считаются верно и анониму отдают 401,
«сколько сберегли» публично и без единого человека внутри, подсказка ников
находит живых людей («liza» → три человека) и молчит на пустой запрос,
дайджест выдаёт 113 за месяц вместо всего каталога.

---

## 29 июля 2026, под утро. Клуб в чате, компактный блок и порядок на главной

Мелкие правки по живым просьбам, все в проде (PR #25).

- **Книжная Клумба переехала в чат**: `CLUB_URL` теперь `t.me/bookclub_mnzh`
  (была страница Notion). Адрес приходит с сервера, поэтому меню бота, команда
  `/club` и плашка на главной поменялись одной настройкой. Проверено с сервера:
  страница отдаёт «Книжная Клумба МнеНеЖалко».
- **«Вместе мы сберегли» свёрнут в строку.** Блок занимал на главной целый
  экран статистики и отодвигал вниз то, ради чего сюда заходят. Теперь одна
  строка с итогом, а цифры и методика раскрываются нажатием и сворачиваются
  обратно; вся строка — кнопка, чтобы попадать пальцем.
- **«О проекте» стала первой плашкой**: человек, попавший сюда впервые, сначала
  должен понять, куда попал, и только потом идти в каталог.
- **Из меню доната убрано «Совсем не обязательно»** — в Mini App этой оговорки
  и не было, теперь тексты совпадают.

*Чем проверено:* тесты блока переписаны под свёрнутое состояние (проверяют, что
цифры и методика спрятаны до нажатия и что блок сворачивается обратно), 280
серверных и 36 интерфейсных зелёные, CI на PR зелёный, бэкап перед деплоем,
health на проде отдаёт новый адрес клуба.

---

## 29 июля 2026, утро. Рисунки заказчика: значки, закладки и инструкция в приложении

Заказчик прислал десять нарисованных значков (`ui/a`) и две закладки-бейджа
(`ui/b`). Коммит `db6482d` (PR #26), всё в проде.

- **Семь значков совпали** с уже существующими один в один (даже «Целая
  библиотека» нарисована с числом 25 — ровно наш порог). Под три оставшихся
  придуманы правила: «Книжный друг» — книги трём **разным людям** (считаются
  люди, а не выдачи), «Хранитель полки» — три книги, вернувшиеся **в срок**
  (только там, где срок вообще договаривались), «Редкая находка» — **чужая**
  пятёрка книге с полки (своя не считается, иначе значок выдаёт себе сам
  владелец).
- **Экран «Мои достижения»**: плашка на главной, ссылка с «Моей полки», на
  самой полке осталась короткая лента «заработанные плюс ближайшая цель».
  У полученного значка подпись «Получено», а не задание «что сделать».
  Картинки сжаты в webp 320px: 2 МБ → 9–14 КБ каждая.
- **Закладки на обложках** вырезаны из тёмного фона в прозрачный webp: маска по
  яркости плюс **морфологическое закрытие**. Без закрытия текст и рисунок внутри
  закладки — это дыры в светлой маске, и вырезка получалась полосатой.
  Показываются только на крупной обложке: в списках она 44 пикселя шириной, и
  подпись превратилась бы в неразличимую полоску. Занятость важнее новизны.
  ⚠️ `.cover img { width: 100% }` растягивал закладку на всю обложку — селектор
  для неё сделан специфичнее.
- **Правило свежести стало общим** для подборки новинок и значка «Новинка»
  (`freshWhere` для запроса, `isFresh` для карточки). Иначе они разошлись бы, а
  при переналивке базы значок получил бы весь каталог — тот самый урок из PR #24,
  теперь закреплённый тестом. На проде: значок у 19 книг из сотни, а не у всех.
- **Инструкция новичка переехала в приложение.** Оригинал (страница Notion)
  предлагал войти по почте, нажать Edit, потом New, вписать себя в таблицу
  Owners, книги — в All Books, а обложку добавить «через Copy image address».
  Сейчас всё это делает бот с одной фотографии, поэтому текст переписан под
  нынешний путь: шесть шагов с кнопками прямо к действию. Обещание из оригинала
  сохранено дословно по смыслу: если лениво или нет времени, пришлите фото,
  админы внесут сами. Текст оригинала выгружен из Notion через `api/v3` с
  токеном проекта — анонимно страница отдаёт только оболочку.

*Чем проверено:* тестов 288 серверных и 39 интерфейсных, новые проверены
откатом. Экраны сняты вживую (собранный `dist` + мок-сервер + Playwright, 390
пикселей): достижения, лента на полке, карточка книги с закладкой, инструкция.
Прод-смоук: 10 значков, из них 4 получено (новый «Книжный друг» сработал на трёх
разных читателях), картинки значков и закладок отдаются с кэшем на сутки.

---

## Что осталось, с причинами

Полный разбор с приоритетами и блокерами вынесен в `docs/TECH_DEBT.md` (27 июля 2026).
Кратко:


| Что | Почему не сделано сейчас |
| --- | --- |
| Baseline-миграции Prisma + индекс «одна активная выдача на книгу» | У проекта **вообще нет** `prisma/migrations`, всё живёт на `db push`. Индекс решено делать один раз вместе с созданием baseline, а не разовой миграцией поверх. |
| Атомарный релиз (`releases/<sha>` + symlink) | Сейчас деплой распаковывает архив поверх живого каталога. Работает, но окно неконсистентности есть. |
| Offsite-бэкап | Заказчик выбрал restic на S3-совместимое хранилище. 🔴 Ждём ключи доступа. |
| Фронтовые тесты (Vitest + RTL) и E2E (Playwright) | Не начаты. Сейчас роботом проверяется только сервер. |
| Приёмка напоминаний о возврате | 🔴 Нужен staging-бот и два живых Telegram-аккаунта. |
| Дизайн-ревью остальных экранов | «Моя полка» приведена к общему стилю 27 июля (см. выше). Дальнейшее ревью имеет смысл делать с живыми скриншотами и участием дизайнерок. |
| Конфиг локаций (вынести всё польское) | Крупный рефактор, отдельным заходом. Сейчас PL-специфика в `seed.ts` и дефолтах `env.ts`. |
| Книжный клуб «Книжная Клумба», редизайн происхождения обложек | Записаны в `docs/BACKLOG_PRODUCT_REQUESTS.md`, намеренно не реализованы. ⚠️ Не путать с уже сделанным конвейером: тот ресайзит **имеющийся** coverUrl, а запрос про то, **откуда** он берётся. |

## Что нужно от заказчика

- **Бот в чат проекта** + выключить privacy mode (или сделать админом). Без этого бот не видит чат:
  `getChat` отдаёт «chat not found», афиши и барахолка остаются пустыми. Диагностика: админская
  `/topics`, плюс бот сам пишет админам при добавлении в чат.
- **Служебный аккаунт Notion** вместо личного (инструкция в README).
- **Живая проверка** добавления: фото стопки книг и полки корешками, альбом обложек, ISBN
  сообщением боту и через поле в Mini App, `/import`, `/onbehalf @ник`.
- **Ограничить ключ Google Books** в Cloud Console только Books API (приходил в переписке).
- **Решение по 21 ГБ на диске VPS**: их держат Docker-образы NestingCenter (`/var/lib/containerd`),
  чистить или нет — не наш проект.
- **Ключи S3** для restic (bucket + keyId + applicationKey).
- ~~Ключ Google Books~~ — **получен и подключён 27 июля** (см. запись выше). ⚠️ Ключ приходил
  сообщением в переписке, поэтому его стоит ограничить в Google Cloud Console только Books API.

## Грабли, которые стоит помнить

- Деплой только с `chown -R mnenezhalko:mnenezhalko` на `server/data`.
- Деплой деплоит HEAD: сначала коммит, потом деплой.
- Локальный запуск в режиме polling забирает апдейты у прода. Локально поднимать только веб-часть
  (`DISABLE_BOT=1`).
- На Windows `prisma generate` падает `EPERM`, если живы осиротевшие процессы `tsx watch` (держат
  `query_engine.dll`). Лечение: убить их, потом generate. Проверено чистым клоном: в изолированной
  среде все команды проходят, то есть это среда, а не код.
- `db push` требует `--accept-data-loss` при добавлении unique-колонки, даже если данных нет.
- `.cover-carousel` обязан быть `position: relative`, иначе `offsetLeft` детей меряется от далёкого
  предка и центрирование врёт.
