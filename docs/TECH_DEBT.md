# Технический долг

Что осознанно не сделано и почему. Обновлено 27 июля 2026.
Продуктовые запросы (новые функции) лежат отдельно: `BACKLOG_PRODUCT_REQUESTS.md`.

Порядок внутри разделов — по убыванию пользы на единицу риска, а не по размеру.

---

## 1. Надёжность выкладки (остаток этапа 10)

Сделано 27 июля: миграции базы вместо `db push`, запрет двух активных выдач одной книги на уровне
БД. Осталось три пункта.

### 1.1. Атомарный релиз
**Что сейчас:** `scripts/deploy.sh` распаковывает архив поверх живого каталога, потом собирает и
рестартует. Между распаковкой и рестартом каталог несогласован: новый код рядом со старой сборкой
Mini App, и в этот момент работающий процесс отдаёт файлы из-под ног.
**Что нужно:** выкладывать в `releases/<sha>`, переключать симлинк `current` одним движением,
рестарт после переключения, хранить 3–5 прошлых релизов для мгновенного отката.
**Почему не сделано сразу:** переделка деплоя рискованнее, чем то, что она чинит; окно
несогласованности сейчас порядка секунд и на практике не стреляло. Делать отдельным заходом,
с проверкой отката на живом сервисе.

### 1.2. Копии данных за пределами сервера
**Что сейчас:** `backup.sh` кладёт базу, обложки и `.env` в `/opt/backups/mnenezhalko` на той же
машине. Диск умрёт вместе с копиями.
**Что нужно:** restic на S3-совместимое хранилище (обсуждали Backblaze B2), проверка
восстановления, отдельные ключи только на запись.
**Блокер:** 🔴 ждём от заказчика ключи доступа (bucket + keyId + applicationKey).

### 1.3. `drop_pending_updates` при старте бота
**Что сейчас:** флаг ставится при установке вебхука. После долгого простоя Telegram может отдать
пачку старых апдейтов, и бот отработает их как свежие.
**Что нужно:** явный флаг в настройках и осознанное решение, что делать с накопленной очередью.
**Цена вопроса:** мелочь, но всплывает после каждого длительного простоя.

---

## 2. Тесты, которых нет

### 2.1. Фронтовые тесты (Vitest + React Testing Library)
**Обновлено 29 июля:** инфраструктура появилась (vitest + jsdom + RTL, `web/src/test-setup.ts`),
первый экран покрыт — отзывы к книгам (9 сценариев, issue #18). Остальные экраны по-прежнему
не покрыты; следующие по важности — «Моя полка» во всех шести состояниях и добавление книги.
**Что сейчас:** роботом проверяется только сервер (132 теста). Логика экранов не покрыта вовсе.
**Почему это дорого обошлось:** регрессия 27 июля (в «Библиотеке» пропали обложки) жила два дня и
её нашёл заказчик, а не мы. Серверный тест на неё я написал, но целый класс поломок
(состояния экрана, пустые списки, ошибки ручек в интерфейсе) по-прежнему никем не ловится.
**Что нужно:** Vitest + RTL на ключевые экраны: «Моя полка» во всех шести состояниях, добавление
книги (фото, ISBN, дубли), выдачи (создание, возврат, отмена).

### 2.2. Сквозные тесты (Playwright)
**Что нужно:** один-два сценария целиком, от открытия Mini App до появления книги на полке,
на собранной версии с подставными данными. Заготовка уже есть: локальный предпросмотр с моками
поднимался в этой сессии (`?screen=myshelf` открывает экран напрямую, Telegram не нужен).

### 2.3. Приёмка напоминаний о возврате
**Что сейчас:** ядро покрыто тестами с инъекцией времени, но живой цепочки «просрочка → сообщение
обеим сторонам → кнопка возврата» никто не проходил.
**Блокер:** 🔴 нужен тестовый бот и два реальных аккаунта Telegram.

---

## 3. Продуктовая логика, где мы знаем о слабом месте

### 3.1. Русские издания по ISBN приходят транслитом
`Istorija Rossii` вместо «История России», автор часто пуст: так эти книги записаны в каталогах,
на которых стоит Google Books. Карточка показывается человеку до сохранения, поэтому это терпимо.
**Что можно:** перед показом пробовать найти книгу по названию в кириллице, либо мягко
предупреждать «название пришло латиницей, проверьте».

### 3.2. Google Books нестабилен
503 `backendFailed` на 6–9 запросов из 10 (замеры с прод-адреса). Обходим повторами (до 4 попыток),
в ответе отличаем «каталог не отвечает» от «книги нет». Если доля сбоев вырастет, поиск по ISBN
для русских изданий фактически перестанет работать — стоит следить.

### 3.3. Обложка книги и «фото-доказательство» — одно и то же поле
Развёрнутый запрос заказчика на пересборку происхождения обложек описан в
`BACKLOG_PRODUCT_REQUESTS.md` (ISBN-first, confidence-scoring, отдельное приватное фото).
Не путать с уже сделанным конвейером: тот уменьшает и кэширует **имеющийся** адрес обложки.

---

## 4. Масштабирование

### 4.1. Конфигурация локации
PL-специфика зашита в `server/src/seed.ts` (города, чат, темы, инстаграм) и в значениях по
умолчанию `env.ts` (идентификаторы таблиц Notion). Перед вторым городом или страной это выносится
в конфиг локации (`locations/pl.ts`), инстанс остаётся отдельным: свой бот, своя база, свои
таблицы. Крупный рефактор, делать отдельным заходом.

### 4.2. Передача сервиса участникам проекта
Отдельный чек-лист: организация на GitHub, два администратора, служебная почта, служебный аккаунт
Notion, `OPERATIONS.md`, регулярный health-check.

---

## 5. Мелочи, которые стоит помнить

- **Служебный аккаунт Notion.** Запись в общую таблицу идёт через cookie личного аккаунта
  заказчика: слетит при смене пароля. 🔴 ждём служебный аккаунт.
- **Ключ Google Books приходил в переписке** — его стоит ограничить в Google Cloud Console только
  Books API.
- **На диске сервера 21 ГБ занимают образы соседнего проекта** (NestingCenter, `/var/lib/containerd`).
  Свободно около 4 ГБ. Не наши данные, чистить только с разрешения владельца.
- **Бот не в чате проекта.** Пока его туда не добавили и не выключили privacy mode, афиши встреч и
  барахолка остаются пустыми. 🔴 ждём заказчика.
