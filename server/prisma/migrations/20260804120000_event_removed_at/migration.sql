-- Снятие прошедшей встречи админом.
-- Мягко: строка остаётся, иначе повторный импорт темы завёл бы встречу заново
-- (дедуп по sourceMsgId), а запись в журнале модерации ссылалась бы в никуда.
ALTER TABLE "Event" ADD COLUMN "removedAt" DATETIME;
