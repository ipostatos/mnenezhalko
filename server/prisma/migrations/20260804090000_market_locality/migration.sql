-- Населённый пункт объявления отдельно от города проекта.
-- Раньше пригород («Wieliczka») уезжал в район и приписывался к заголовку,
-- а город становился «Все города» — объявление выпадало из фильтра по городу.
-- Колонка nullable: у прежних строк пригород неизвестен, и выдумывать его нельзя.
ALTER TABLE "MarketItem" ADD COLUMN "locality" TEXT;
