# скрипты и юниты уезжают на Linux — только LF, иначе `bash: cannot execute`
*.sh text eol=lf
*.service text eol=lf
Caddyfile.snippet text eol=lf
*.timer text eol=lf
# проверка монитора запускается на Linux-бегуне GitHub и на сервере
*.mjs text eol=lf
